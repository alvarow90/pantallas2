<Shell
    x:Class="pantallas.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:pantallas"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate local:Dashboard}"
            Route="Dashboard" />

        <ShellContent
            Title="Entrenamiento"
            ContentTemplate="{DataTemplate local:Entrenador}" 
            Route="Entrenamiento" />

        <ShellContent
            Title="Nutrición"
            ContentTemplate="{DataTemplate local:Nutricion}"
            Route="Nutricion" />

        <ShellContent
            Title="Chat"
            ContentTemplate="{DataTemplate local:Chat}"
            Route="Chat" />
    </TabBar>
</Shell>
